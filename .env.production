# Production Environment Configuration
# This file is used when you run: npm run build

# Option 1 (RECOMMENDED): Deploy React app on same server as backend
# Use relative URL to avoid CORS issues entirely
REACT_APP_API_URL=http://31.97.70.3:8080/event-manage

# Option 2: If React app is on different domain and backend CORS is properly configured
# Use full URL:
# REACT_APP_API_URL=http://31.97.70.3:8080/event-manage

# Disable source maps in production for security
GENERATE_SOURCEMAP=false
